import { Typography, Container } from '@mui/material';

const CatalogViewPage = () => {
  return (
    <Container>
      <Typography variant="h4" component="h1" gutterBottom>
        Página de Vista de Catálogo
      </Typography>
    </Container>
  );
};

export default CatalogViewPage;
